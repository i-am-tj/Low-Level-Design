package Structural.Adapter.v1;

public class Client {
    public static void main(String[] args) {
        // Whenever you are connecting to a 3rd party library don't connect to it directly as it violates DI principle
        // Instead connect ot it via an interface in between

        // 1. Create an interface with methods that you need from the 3rd party
    }
}
